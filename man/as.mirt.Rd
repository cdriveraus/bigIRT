% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mirt-bridge.R
\name{as.mirt}
\alias{as.mirt}
\title{Coerce a fit to a fixed-parameter mirt object}
\usage{
as.mirt(fit, max_cells = 5e+07, verbose = FALSE)
}
\arguments{
\item{fit}{A fitted object from \code{\link[=fitIRT]{fitIRT()}}.}

\item{max_cells}{Numeric. Refuse a wide matrix larger than this.}

\item{verbose}{Logical. Passed to \code{mirt::mirt()}.}
}
\value{
A \code{SingleGroupClass} object with every parameter fixed at this
fit's values.
}
\description{
Opens mirt's diagnostic suite -- \code{itemfit()}, \code{personfit()}, \code{M2()},
\code{residuals(type = "Q3")}, \code{itemplot()}, \code{DIF()} -- none of which is worth
reimplementing, because all of them need the wide person-by-item matrix and
that, not the arithmetic, is the binding constraint. Data too large to hold
as one cannot cross, and is refused rather than attempted.
}
\seealso{
\code{\link[=checkMirtBridge]{checkMirtBridge()}}, which verifies the translation.
}
