% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{heldoutMetrics}
\alias{heldoutMetrics}
\title{Predictive performance on held-out responses}
\usage{
heldoutMetrics(fit, eps = 1e-12)
}
\arguments{
\item{fit}{A fitted object from \code{\link[=fitIRT]{fitIRT()}}, fitted with \code{trainingRows}.}

\item{eps}{Numeric. Probability clamp for the log loss.}
}
\value{
A list of held-out response count, log loss, Brier score, AUC,
accuracy and the item-base-rate Brier score, or \code{NULL} when nothing was
held out.
}
\description{
Scores the rows excluded by \code{trainingRows} against the item base rate, which
predicts each item's training success rate and ignores the person. That is
the fitted model with zero discrimination, so it lies inside the model's own
parameter space and is a fair floor.
}
