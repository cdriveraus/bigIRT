% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitIRT.R
\name{fitIRT}
\alias{fitIRT}
\title{Title}
\usage{
fitIRT(
  dat,
  score = "score",
  id = "id",
  item = "Item",
  scale = "Scale",
  pl = 1,
  Adata = NA,
  Bdata = NA,
  Cdata = NA,
  Abilitydata = NA,
  logAMeandat = 0.542,
  logASD = 2,
  BMeandat = 0,
  BSD = 5,
  logitCMeandat = -5,
  logitCSD = 2,
  AbilityMeandat = array(0, dim = c(length(unique(dat[[scale]])))),
  AbilitySD = array(5, dim = c(length(unique(dat[[scale]])))),
  AMeanSD = 0.01,
  BMeanSD = BSD,
  logitCMeanSD = logitCSD,
  ABMeandat = 0,
  ABSD = BSD,
  AbilityMeanSD = array(0.01, dim = c(length(unique(dat[[scale]])))),
  iter = 2000,
  cores = 6,
  carefulfit = TRUE,
  ebayes = TRUE,
  ebayesmultiplier = 2,
  estMeans = FALSE,
  priors = TRUE,
  outlierfix = TRUE,
  outlierscale = 2,
  normalise = TRUE,
  AB = FALSE,
  ...
)
}
\arguments{
\item{...}{}
}
\value{

}
\description{
Title
}
\examples{
#Generate some data (here 2pl model
require(data.table)
dat <- bigIRT:::IRTsim(Nsubs = 5000,Nitems = 100,Nscales = 1,
  logitCMean = -10,logitCSD = 0,logAMean = 1,logASD = .3,
  BMean=0,BSD = .5,
  AbilityMean = 0,AbilitySD = 1)

#convert to wide for TAM
wdat <- data.frame(dcast(data.table(dat$dat),formula = 'id ~ Item',value.var='score')[,-1])


#fit using TAM
require(TAM)
tfit <-tam.mml.2pl(resp = wdat,est.variance = TRUE)


#fit using bigIRT
fit <- fitIRT(dat$dat,cores=2,score = 'score',id = 'id',
  scale = 'Scale',item = 'Item', pl=2)
}
