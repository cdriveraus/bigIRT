% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{looIRT}
\alias{looIRT}
\title{Approximate leave-one-out cross-validation}
\usage{
looIRT(fit, draws = 500L, persons = NULL, seed = NULL, cores = 1L)
}
\arguments{
\item{fit}{A fitted object from \code{\link[=fitIRT]{fitIRT()}}, fitted with
\code{laplaceKeepCovariance = TRUE}.}

\item{draws}{Integer. Posterior draws per person.}

\item{persons}{Integer. Optional person subsample, for datasets too large to
hold a draws-by-responses matrix.}

\item{seed}{Integer. Optional seed for the draws.}

\item{cores}{Integer. Passed to \code{loo::loo()}.}
}
\value{
An object of class \code{loo}.
}
\description{
The Laplace backend leaves a Gaussian posterior for each person, so draws
from it can be importance-weighted and handed to \code{loo}. Item parameters stay
at their mode, so the result is conditional on them; that is mild when
persons greatly outnumber the responses per item. Pareto k is the check that
matters, and held-out cross-validation is the better tool when it is large.
}
\seealso{
\code{\link[=heldoutMetrics]{heldoutMetrics()}}
}
